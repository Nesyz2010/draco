repeat task.wait() until game:IsLoaded()
repeat task.wait() until game.Players and game.Players.LocalPlayer

------------------------------------------------------------
-- 🧩 LOAD UI FLUENT
------------------------------------------------------------
local Fluent = loadstring(game:HttpGet(
    "https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"
))()

local Window = Fluent:CreateWindow({
    Title = "Draco Hub",
    SubTitle = "HoanqLamDepTrai",
    TabWidth = 160,
    Size = UDim2.fromOffset(530, 350),
    Acrylic = false,
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.End
})

------------------------------------------------------------
-- 🧩 TAB SHOP
------------------------------------------------------------
local Tabs = {
    Shop = Window:AddTab({ Title = "Shop" }),
}

------------------------------------------------------------
-- 🧩 FUNCTION TWEEN CHUNG
------------------------------------------------------------
local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local SPEED = 260

local function toposition(Pos, onDone)
    local plr = Players.LocalPlayer
    local char = plr.Character or plr.CharacterAdded:Wait()
    local hrp = char:WaitForChild("HumanoidRootPart")
    local hum = char:FindFirstChildOfClass("Humanoid")

    local root = char:FindFirstChild("Root")
    if not root then
        root = Instance.new("Part")
        root.Size = Vector3.new(20, 0.5, 20)
        root.Name = "Root"
        root.Anchored = true
        root.Transparency = 1
        root.CanCollide = false
        root.CFrame = hrp.CFrame
        root.Parent = char
    end

    if hum and hum.Sit then hum.Sit = false end

    local dist = (Pos.Position - hrp.Position).Magnitude
    local info = TweenInfo.new(dist / SPEED, Enum.EasingStyle.Linear)

    if dist <= 10 then
        hrp.CFrame = Pos
        if onDone then onDone() end
        return
    end

    local tween = TweenService:Create(root, info, { CFrame = Pos })
    tween:Play()

    local running = true

    task.spawn(function()
        while running and tween.PlaybackState == Enum.PlaybackState.Playing do
            task.wait()
            hrp.CFrame = root.CFrame
        end
    end)

    tween.Completed:Once(function()
        running = false
        hrp.CFrame = root.CFrame
        if onDone then onDone() end
    end)
end

------------------------------------------------------------
-- 🧩 BUY SHARKMAN KARATE
------------------------------------------------------------
local sharkmanPos = CFrame.new(-4972.2798, 314.5509, -3221.4897)

local function buySharkman()
    pcall(function()
        ReplicatedStorage.Remotes.CommF_:InvokeServer("BuySharkmanKarate")
    end)
    warn("[Sharkman] Remote sent!")
end

Tabs.Shop:AddButton({
    Title = "Buy Sharkman Karate",
    Description = "Tween + Buy Sharkman Karate",
    Callback = function()
        toposition(sharkmanPos, function()
            task.wait(0.2)
            buySharkman()
        end)
    end
})

------------------------------------------------------------
-- 🧩 BUY DRAGON TALON
------------------------------------------------------------
local dragonPos = CFrame.new(5776.378, 1209.483, 805.036)

local function buyDragonTalon()
    pcall(function()
        ReplicatedStorage.Remotes.CommF_:InvokeServer("BuyDragonTalon")
    end)
    warn("[Dragon Talon] Remote sent!")
end

Tabs.Shop:AddButton({
    Title = "Buy Dragon Talon",
    Description = "Tween + Buy Dragon Talon",
    Callback = function()
        toposition(dragonPos, function()
            task.wait(0.2)
            buyDragonTalon()
        end)
    end
})

------------------------------------------------------------
-- 🧩 ICON BẬT / TẮT UI — LOGO CAT HUB CŨ
------------------------------------------------------------
local ToggleGui = Instance.new("ScreenGui")
ToggleGui.Name = "ToggleUI"
ToggleGui.Parent = game.CoreGui
ToggleGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

local ToggleBtn = Instance.new("ImageButton")
ToggleBtn.Name = "ToggleButton"
ToggleBtn.Parent = ToggleGui
ToggleBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
ToggleBtn.BorderSizePixel = 0
ToggleBtn.Size = UDim2.new(0, 50, 0, 50)
ToggleBtn.Position = UDim2.new(0.12, 0, 0.09, 0)

-- ⭐ Logo Cat Hub cũ
ToggleBtn.Image = "rbxassetid://14997704044"

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 10)
UICorner.Parent = ToggleBtn

ToggleBtn.MouseButton1Click:Connect(function()
    game:GetService("VirtualInputManager"):SendKeyEvent(true, Enum.KeyCode.End, false, game)
end)
