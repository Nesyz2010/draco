repeat task.wait() until game:IsLoaded()
repeat task.wait() until game.Players and game.Players.LocalPlayer

------------------------------------------------------------
-- 🧩 LOAD FLUENT UI
------------------------------------------------------------
local Fluent = loadstring(game:HttpGet(
    "https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"
))()

local Window = Fluent:CreateWindow({
    Title = "Draco Hub",
    SubTitle = "HoanqLamDepTrai",
    TabWidth = 160,
    Size = UDim2.fromOffset(530, 350),
    Acrylic = false,
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.End
})

------------------------------------------------------------
-- 🧩 CREATE TABS
------------------------------------------------------------
local Tabs = {
    Shop      = Window:AddTab({ Title = "Shop" }),
    Draco     = Window:AddTab({ Title = "Draco" }),
    Belt      = Window:AddTab({ Title = "Dojo Belt" }),
    Orange    = Window:AddTab({ Title = "Dojo Belt (Orange)" }), -- ⭐ TAB MỚI
    Teleport  = Window:AddTab({ Title = "Teleport" }),
}

------------------------------------------------------------
-- 🧩 TWEEN MOVE FUNCTION
------------------------------------------------------------
local TweenService = game:GetService("TweenService")
local Players      = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local SPEED = 260

local function toposition(Pos, onDone)
    local plr  = Players.LocalPlayer
    local char = plr.Character or plr.CharacterAdded:Wait()
    local hrp  = char:WaitForChild("HumanoidRootPart")
    local hum  = char:FindFirstChildOfClass("Humanoid")

    local root = char:FindFirstChild("Root")
    if not root then
        root = Instance.new("Part")
        root.Name = "Root"
        root.Anchored = true
        root.Transparency = 1
        root.CanCollide = false
        root.Size = Vector3.new(20, 0.5, 20)
        root.CFrame = hrp.CFrame
        root.Parent = char
    end

    if hum and hum.Sit then hum.Sit = false end

    local dist = (Pos.Position - hrp.Position).Magnitude
    local info = TweenInfo.new(dist / SPEED, Enum.EasingStyle.Linear)

    if dist <= 10 then
        hrp.CFrame = Pos
        if onDone then onDone() end
        return
    end
    
    local tween = TweenService:Create(root, info, { CFrame = Pos })
    tween:Play()

    task.spawn(function()
        while tween.PlaybackState == Enum.PlaybackState.Playing do
            task.wait()
            hrp.CFrame = root.CFrame
        end
    end)

    tween.Completed:Once(function()
        hrp.CFrame = root.CFrame
        if onDone then onDone() end
    end)
end

------------------------------------------------------------
-- 🧩 SHOP — BUY SHARKMAN
------------------------------------------------------------
local sharkPos = CFrame.new(-4972.2798, 314.5509, -3221.4897)

Tabs.Shop:AddButton({
    Title = "Buy Sharkman Karate",
    Description = "Move → Buy Sharkman",
    Callback = function()
        toposition(sharkPos, function()
            task.wait(.2)
            pcall(function()
                ReplicatedStorage.Remotes.CommF_:InvokeServer("BuySharkmanKarate")
            end)
        end)
    end
})

------------------------------------------------------------
-- 🧩 SHOP — BUY DRAGON TALON
------------------------------------------------------------
local dragonPos = CFrame.new(5776.378, 1209.483, 805.036)

Tabs.Shop:AddButton({
    Title = "Buy Dragon Talon",
    Description = "Move → Buy Dragon Talon",
    Callback = function()
        toposition(dragonPos, function()
            task.wait(.2)
            pcall(function()
                ReplicatedStorage.Remotes.CommF_:InvokeServer("BuyDragonTalon")
            end)
        end)
    end
})

------------------------------------------------------------
-- 🧩 DRACO — AUTO DOJO QUEST
------------------------------------------------------------
local dojoPos = CFrame.new(5865.9468, 1208.5947, 872.6288)
local RF = ReplicatedStorage.Modules.Net["RF/InteractDragonQuest"]

local function dojoRemote(cmd)
    pcall(function()
        RF:InvokeServer({ NPC = "Dojo Trainer", Command = cmd })
    end)
end

local function autoDojo()
    toposition(dojoPos, function()
        task.wait(.3)

        local state = Players.LocalPlayer.Data.Dojos.Quest.Value

        if state == "None" then
            dojoRemote("RequestQuest")
        elseif state == "Complete" then
            dojoRemote("CompleteQuest")
        else
            print("[Dojo] Đang làm quest…")
        end
    end)
end

Tabs.Draco:AddButton({
    Title = "Auto Dojo Quest",
    Description = "Auto nhận & trả quest",
    Callback = autoDojo
})

------------------------------------------------------------
-- 🧩 DRACO — LEARN TETHER
------------------------------------------------------------
Tabs.Draco:AddButton({
    Title = "Learn Tether",
    Description = "Học Tether ngay",
    Callback = function()
        toposition(dragonPos, function()
            task.wait(.2)
            pcall(function()
                RF:InvokeServer({ NPC = "Dragon Wizard", Command = "LearnTether" })
            end)
        end)
    end
})

------------------------------------------------------------
-- 🧩 DOJO BELT TAB — CLOSET / PICKUP / DROP FRUIT
------------------------------------------------------------

-- Closet Fruit
Tabs.Belt:AddButton({
    Title = "Closet Fruit",
    Description = "Move → Fruit Closet",
    Callback = function()
        toposition(CFrame.new(
            -12550.0712890625,
            337.4825744628906,
            -7485.72216796875
        ))
    end
})

-- Lụm Fruit Gần Nhất
Tabs.Belt:AddButton({
    Title = "Lụm Fruit Gần Nhất",
    Description = "Move → Pickup nearest fruit",
    Callback = function()

        local lp = Players.LocalPlayer
        local char = lp.Character
        local hrp = char:FindFirstChild("HumanoidRootPart")
        if not hrp then return end

        local nearest, minDist = nil, math.huge

        for _, obj in ipairs(workspace:GetDescendants()) do
            if obj:IsA("Tool") and obj:FindFirstChild("Handle") then
                local dist = (obj.Handle.Position - hrp.Position).Magnitude
                if dist < minDist then
                    minDist = dist
                    nearest = obj
                end
            end
        end

        if not nearest then
            warn("[Fruit] Không tìm thấy trái.")
            return
        end

        toposition(nearest.Handle.CFrame, function()
            task.wait(.1)
            pcall(function()
                firetouchinterest(hrp, nearest.Handle, 0)
                task.wait()
                firetouchinterest(hrp, nearest.Handle, 1)
            end)
        end)
    end
})

-- Drop Fruit
Tabs.Belt:AddButton({
    Title = "Drop Fruit",
    Description = "Thả trái đang giữ",
    Callback = function()

        local plr = Players.LocalPlayer
        local char = plr.Character
        local backpack = plr.Backpack

        local tool = char:FindFirstChildWhichIsA("Tool") or 
                     backpack:FindFirstChildWhichIsA("Tool")

        if not tool then
            warn("[Fruit] Không có trái để drop.")
            return
        end

        if tool.Parent == backpack then
            char.Humanoid:EquipTool(tool)
            task.wait(.1)
        end

        pcall(function()
            tool.Parent = workspace
            tool.Handle.CFrame = char.HumanoidRootPart.CFrame
        end)
    end
})

------------------------------------------------------------
-- 🧩 DOJO BELT (ORANGE) — NEW TAB
------------------------------------------------------------

Tabs.Orange:AddButton({
    Title = "TP Ogren Belt",
    Description = "Move → Ogren Belt Position",
    Callback = function()
        toposition(CFrame.new(
            -12591.05859375,
            337.62872314453125,
            -7544.7568359375
        ))
    end
})

------------------------------------------------------------
-- 🧩 TELEPORT TAB — 3 MAIN LOCATIONS
------------------------------------------------------------
Tabs.Teleport:AddButton({
    Title = "Mansion (Sea 3)",
    Description = "Move to Mansion",
    Callback = function()
        toposition(CFrame.new(
            -12549.6630859375,
            337.4825744628906,
            -7531.4306640625
        ))
    end
})

Tabs.Teleport:AddButton({
    Title = "Hydra (Blue Belt 1)",
    Description = "Move to Hydra B1",
    Callback = function()
        toposition(CFrame.new(
            5817.47705078125,
            1208.5947265625,
            904.3050537109375
        ))
    end
})

Tabs.Teleport:AddButton({
    Title = "Hydra (Blue Belt 2)",
    Description = "Move to Hydra B2",
    Callback = function()
        toposition(CFrame.new(
            5816.978515625,
            1208.5947265625,
            898.1947631835938
        ))
    end
})

------------------------------------------------------------
-- 🧩 UI TOGGLE BUTTON (CAT HUB ICON)
------------------------------------------------------------
local ToggleGui = Instance.new("ScreenGui", game.CoreGui)

local Btn = Instance.new("ImageButton", ToggleGui)
Btn.Size = UDim2.new(0,50,0,50)
Btn.Position = UDim2.new(0.12,0,0.09,0)
Btn.Image = "rbxassetid://14997704044"
Btn.BackgroundColor3 = Color3.fromRGB(255,255,255)

Instance.new("UICorner", Btn).CornerRadius = UDim.new(0,10)

Btn.MouseButton1Click:Connect(function()
    local vim = game:GetService("VirtualInputManager")
    vim:SendKeyEvent(true, Enum.KeyCode.End, false, game)
    task.wait(.05)
    vim:SendKeyEvent(false, Enum.KeyCode.End, false, game)
end)
